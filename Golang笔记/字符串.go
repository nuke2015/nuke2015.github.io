//转小写
strings.ToLower();
//数组长度;
len("hello")
//数组调用
params[0]

//字符串数组做参数
func (this *baseController) setHeadMetas(params ...string) {}

//把数组用-转成字符串
strings.Join(title_buf, " - ")

//字符转整形
strconv.Atoi()

//字符串转数组;
arr := strings.Split(this.Ctx.GetCookie("auth"), "|")

//字符串转整形并带范围
strconv.ParseInt(idstr, 10, 0)

//模板格式化
fmt.Sprintf("/admin/article/list?status=%d&searchtype=%s&keyword=%s", status, searchtype, keyword), true).ToString()

//去其它;
strings.Trim(post.Tags, ",")

//去空格
title   string = strings.TrimSpace(this.GetString("title"))

status  int64  = 0
//小整形
istop   int8   = 0

